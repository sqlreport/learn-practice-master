# Codespace Environment Variables
# This file is automatically created by the devcontainer setup

# Database Configuration
DATABASE_URL=postgresql://myapp_user:myapp_password@db:5432/myapp_db
POSTGRES_PASSWORD=myapp_password

# Flask Security (development keys - never use in production!)
SECRET_KEY=dev-secret-key-for-codespace
JWT_SECRET_KEY=dev-jwt-secret-key-for-codespace

# Flask Environment
FLASK_ENV=development
FLASK_DEBUG=1

# Frontend Configuration (dynamically set in scripts)
VITE_API_URL=http://localhost:5000/api

# PgAdmin (optional)
PGADMIN_DEFAULT_EMAIL=admin@codespace.dev
PGADMIN_DEFAULT_PASSWORD=admin123

# SSL (not needed for development)
SSL_CERT_PATH=
SSL_KEY_PATH=

# Note: This file contains development-only secrets.
# Never use these values in production!